# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: accessibility/accessibility.spec.js >> Accessibility - Color Contrast Tests >> Dark Mode >> /tools/csv-table (dark mode) has sufficient color contrast
- Location: tests/e2e/accessibility/accessibility.spec.js:170:7

# Error details

```
Error: expect(received).toHaveLength(expected)

Expected length: 0
Received length: 1
Received array:  [{"description": "Ensure the contrast between foreground and background colors meets WCAG 2 AA minimum contrast ratio thresholds", "help": "Elements must meet minimum color contrast ratio thresholds", "helpUrl": "https://dequeuniversity.com/rules/axe/4.12/color-contrast?application=playwright", "id": "color-contrast", "impact": "serious", "nodes": [{"all": [], "any": [{"data": {"bgColor": "#ffffff", "contrastRatio": 2.59, "expectedContrastRatio": "3:1", "fgColor": "#f5821f", "fontSize": "72.0pt (96px)", "fontWeight": "bold", "messageKey": null}, "id": "color-contrast", "impact": "serious", "message": "Element has insufficient color contrast of 2.59 (foreground color: #f5821f, background color: #ffffff, font size: 72.0pt (96px), font weight: bold). Expected contrast ratio of 3:1", "relatedNodes": [{"html": "<body>
    <div class=\"code\">502</div>
    <div class=\"message\">Oops! Bad Gateway</div>
    <div class=\"hint\">Something went wrong. It's not you, it's us.</div>····
</body>", "target": ["body"]}]}], "failureSummary": "Fix any of the following:
  Element has insufficient color contrast of 2.59 (foreground color: #f5821f, background color: #ffffff, font size: 72.0pt (96px), font weight: bold). Expected contrast ratio of 3:1", "html": "<div class=\"code\">502</div>", "impact": "serious", "none": [], "target": [".code"]}], "tags": ["cat.color", "wcag2aa", "wcag143", "TTv5", "TT13.c", "EN-301-549", "EN-9.1.4.3", "ACT", "RGAAv4", "RGAA-3.2.1"]}]
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e2]: "502"
  - generic [ref=e3]: Oops! Bad Gateway
  - generic [ref=e4]: Something went wrong. It's not you, it's us.
```

# Test source

```ts
  95  | 
  96  | // Helper to wait for dynamic content on specific pages
  97  | async function waitForDynamicContent(page, url) {
  98  |   if (url.includes('/tools/url-checker?url=')) {
  99  |     // Wait for URL checker results to load
  100 |     await page.waitForSelector('#resultsCard:not(.d-none)', { timeout: 15000 }).catch(() => {});
  101 |     await page.waitForTimeout(1000); // Extra time for all results to render
  102 |   }
  103 |   if (url.includes('/tools/dns-lookup?domain=')) {
  104 |     // Wait for DNS lookup results to load
  105 |     await page.waitForSelector('#resultsContainer:not(.d-none)', { timeout: 15000 }).catch(() => {});
  106 |     await page.waitForTimeout(1000); // Extra time for all results to render
  107 |   }
  108 | }
  109 | 
  110 | // Helper function to run axe analysis
  111 | async function runAccessibilityAudit(page, options = {}) {
  112 |   const axeBuilder = new AxeBuilder({ page })
  113 |     .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa']);
  114 |   
  115 |   // Focus on color contrast rules if specified
  116 |   if (options.contrastOnly) {
  117 |     axeBuilder.withRules(['color-contrast', 'color-contrast-enhanced']);
  118 |   }
  119 |   
  120 |   return await axeBuilder.analyze();
  121 | }
  122 | 
  123 | // Helper to format violations for readable output
  124 | function formatViolations(violations) {
  125 |   return violations.map(v => ({
  126 |     rule: v.id,
  127 |     impact: v.impact,
  128 |     description: v.description,
  129 |     helpUrl: v.helpUrl,
  130 |     nodes: v.nodes.map(n => ({
  131 |       html: n.html.substring(0, 200),
  132 |       failureSummary: n.failureSummary,
  133 |     })),
  134 |   }));
  135 | }
  136 | 
  137 | test.describe('Accessibility - Color Contrast Tests', () => {
  138 |   
  139 |   test.describe('Light Mode', () => {
  140 |     for (const toolUrl of ALL_URLS) {
  141 |       test(`${toolUrl} has sufficient color contrast`, async ({ page }) => {
  142 |         await page.goto(toolUrl);
  143 |         
  144 |         // Wait for page to fully load
  145 |         await page.waitForLoadState('networkidle');
  146 |         
  147 |         // Wait for dynamic content on specific pages
  148 |         await waitForDynamicContent(page, toolUrl);
  149 |         
  150 |         // Run axe analysis focusing on color contrast
  151 |         const results = await runAccessibilityAudit(page, { contrastOnly: true });
  152 |         
  153 |         // Log violations for debugging
  154 |         if (results.violations.length > 0) {
  155 |           console.log(`\nContrast violations on ${toolUrl}:`);
  156 |           console.log(JSON.stringify(formatViolations(results.violations), null, 2));
  157 |         }
  158 |         
  159 |         // Assert no color contrast violations (WCAG AA only, not AAA)
  160 |         expect(results.violations.filter(v => 
  161 |           v.id === 'color-contrast'
  162 |         )).toHaveLength(0);
  163 |       });
  164 |     }
  165 |   });
  166 | 
  167 |   // Dark mode tests
  168 |   test.describe('Dark Mode', () => {
  169 |     for (const toolUrl of ALL_URLS) {
  170 |       test(`${toolUrl} (dark mode) has sufficient color contrast`, async ({ page }) => {
  171 |         await page.goto(toolUrl);
  172 |         
  173 |         // Wait for page to fully load and dynamic content
  174 |         await page.waitForLoadState('networkidle');
  175 |         await waitForDynamicContent(page, toolUrl);
  176 |         
  177 |         // Enable dark mode (adjust selector based on implementation)
  178 |         await page.evaluate(() => {
  179 |           document.documentElement.setAttribute('data-bs-theme', 'dark');
  180 |         });
  181 |         
  182 |         // Wait for styles to apply
  183 |         await page.waitForTimeout(100);
  184 |         
  185 |         const results = await runAccessibilityAudit(page, { contrastOnly: true });
  186 |         
  187 |         if (results.violations.length > 0) {
  188 |           console.log(`\nDark mode contrast violations on ${toolUrl}:`);
  189 |           console.log(JSON.stringify(formatViolations(results.violations), null, 2));
  190 |         }
  191 |         
  192 |         // Assert no color contrast violations (WCAG AA only, not AAA)
  193 |         expect(results.violations.filter(v => 
  194 |           v.id === 'color-contrast'
> 195 |         )).toHaveLength(0);
      |            ^ Error: expect(received).toHaveLength(expected)
  196 |       });
  197 |     }
  198 |   });
  199 | 
  200 |   // High Contrast (AAA) mode tests
  201 |   test.describe('High Contrast Mode (AAA)', () => {
  202 |     for (const toolUrl of ALL_URLS) {
  203 |       test(`${toolUrl} (high-contrast) meets WCAG AAA contrast`, async ({ page }) => {
  204 |         await page.goto(toolUrl);
  205 |         
  206 |         // Wait for page to fully load and dynamic content
  207 |         await page.waitForLoadState('networkidle');
  208 |         await waitForDynamicContent(page, toolUrl);
  209 |         
  210 |         // Enable high-contrast mode
  211 |         await page.evaluate(() => {
  212 |           document.documentElement.setAttribute('data-bs-theme', 'high-contrast');
  213 |         });
  214 |         
  215 |         // Wait for styles to apply
  216 |         await page.waitForTimeout(100);
  217 |         
  218 |         const results = await runAccessibilityAudit(page, { contrastOnly: true });
  219 |         
  220 |         if (results.violations.length > 0) {
  221 |           console.log(`\nHigh contrast AAA violations on ${toolUrl}:`);
  222 |           console.log(JSON.stringify(formatViolations(results.violations), null, 2));
  223 |         }
  224 |         
  225 |         // For AAA mode, check both AA and AAA contrast rules
  226 |         expect(results.violations.filter(v => 
  227 |           v.id === 'color-contrast' || v.id === 'color-contrast-enhanced'
  228 |         )).toHaveLength(0);
  229 |       });
  230 |     }
  231 |   });
  232 | });
  233 | 
  234 | test.describe('Accessibility - Full WCAG Audit', () => {
  235 |   
  236 |   // Run a full accessibility audit on the homepage
  237 |   test('Homepage passes WCAG 2.1 AA', async ({ page }) => {
  238 |     await page.goto('/');
  239 |     await page.waitForLoadState('networkidle');
  240 |     
  241 |     const results = await runAccessibilityAudit(page);
  242 |     
  243 |     if (results.violations.length > 0) {
  244 |       console.log('\nHomepage accessibility violations:');
  245 |       console.log(JSON.stringify(formatViolations(results.violations), null, 2));
  246 |     }
  247 |     
  248 |     // For now, just log - can make strict later
  249 |     expect(results.violations).toBeDefined();
  250 |   });
  251 | 
  252 |   // Sample a few key tools for full audit
  253 |   const sampleTools = ['/tools/base64', '/tools/json-formatter', '/tools/clock'];
  254 |   
  255 |   for (const toolUrl of sampleTools) {
  256 |     test(`${toolUrl} passes WCAG 2.1 AA`, async ({ page }) => {
  257 |       await page.goto(toolUrl);
  258 |       await page.waitForLoadState('networkidle');
  259 |       
  260 |       const results = await runAccessibilityAudit(page);
  261 |       
  262 |       if (results.violations.length > 0) {
  263 |         console.log(`\nAccessibility violations on ${toolUrl}:`);
  264 |         console.log(JSON.stringify(formatViolations(results.violations), null, 2));
  265 |       }
  266 |       
  267 |       // Log but don't fail for now - use this to identify issues
  268 |       expect(results.violations).toBeDefined();
  269 |     });
  270 |   }
  271 | });
  272 | 
  273 | test.describe('Accessibility - Contrast Ratio Report', () => {
  274 |   
  275 |   test('Generate contrast report for all tools', async ({ page }) => {
  276 |     const report = {
  277 |       timestamp: new Date().toISOString(),
  278 |       tools: [],
  279 |     };
  280 |     
  281 |     // Test a subset for the report (full list would be slow)
  282 |     const toolsToTest = TOOL_URLS.slice(0, 10);
  283 |     
  284 |     for (const toolUrl of toolsToTest) {
  285 |       try {
  286 |         await page.goto(toolUrl);
  287 |         await page.waitForLoadState('networkidle');
  288 |         
  289 |         const results = await runAccessibilityAudit(page, { contrastOnly: true });
  290 |         
  291 |         report.tools.push({
  292 |           url: toolUrl,
  293 |           passed: results.violations.length === 0,
  294 |           violationCount: results.violations.length,
  295 |           violations: results.violations.map(v => ({
```